/**
 * Utilities for building hashes. All base method classes should provide
 * the same static utility methods so a static import allows for a compile
 * time choice of technique.
 */

package roquen.hash;
